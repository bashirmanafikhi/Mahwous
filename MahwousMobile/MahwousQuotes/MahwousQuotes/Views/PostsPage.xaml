<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:templates="clr-namespace:MahwousQuotes.Views.DataTemplates"
             xmlns:vm="clr-namespace:MahwousQuotes.ViewModels"
             mc:Ignorable="d"
             x:Class="MahwousQuotes.Views.PostsPage"
             Visual="Material"
             Title="{Binding Title}">



    <RefreshView IsRefreshing="{Binding IsBusy, Mode=TwoWay}" Command="{Binding LoadPostsCommand}">
        <StackLayout>
            <CollectionView x:Name="ItemsCollectionView"
                            RemainingItemsThreshold="{Binding ItemTreshold}"
                            RemainingItemsThresholdReachedCommand="{Binding LoadMorePostsCommand}"
                            ItemTemplate="{DataTemplate templates:PostTemplate}"
                ItemsSource="{Binding Posts}">

            </CollectionView>
            <ActivityIndicator IsRunning="{Binding IsLoadingMore}"  IsVisible="{Binding IsLoadingMore}" />


        </StackLayout>
    </RefreshView>


</ContentPage>