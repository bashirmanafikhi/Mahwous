<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MahwousQuotes.ViewModels"
             x:Class="MahwousQuotes.Views.LikedQuotesPage"
             xmlns:templates="clr-namespace:MahwousQuotes.Views.DataTemplates"
             Title="فيديوهات أعجبتك">

    <ContentPage.BindingContext>
        <vm:LikedQuotesViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>

            <Frame CornerRadius="30" 
                    Margin="40,0" 
                    Padding="5"
                    BackgroundColor="{DynamicResource colRed}" 
                    HasShadow="True">

                <Label Text="فيديوهات أعجبتك" 
                    VerticalOptions="CenterAndExpand"
                    FontAttributes="Bold"
                    TextColor="{DynamicResource colWhite}"
                    HorizontalOptions="Center" />
            </Frame>


            <CollectionView x:Name="ItemsCollectionView"
                            ItemTemplate="{DataTemplate templates:QuoteTemplate}"
                            ItemsSource="{Binding Quotes}">

            </CollectionView>
            <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}"
                       Color="{DynamicResource colRed}"/>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>