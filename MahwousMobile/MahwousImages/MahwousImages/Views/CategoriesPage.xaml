<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:MahwousImages.ViewModels"
             xmlns:templates="clr-namespace:MahwousImages.Views.DataTemplates"
             mc:Ignorable="d"
             x:Class="MahwousImages.Views.CategoriesPage"
             Title="{Binding Title}" >


    <ContentPage.BindingContext>
        <vm:CategoriesViewModel />
    </ContentPage.BindingContext>

    <RefreshView IsRefreshing="{Binding IsBusy, Mode=TwoWay}" 
                 Command="{Binding LoadCategoriesCommand}">
        <CollectionView x:Name="ItemsCollectionView"
                    ItemTemplate="{DataTemplate templates:CategoryTemplate}"
                    ItemsSource="{Binding Categories}">

            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2"/>
            </CollectionView.ItemsLayout>

            <!--<CollectionView.ItemTemplate>
                <DataTemplate>
                    <templates:CategoryTemplate/>
                </DataTemplate>
            </CollectionView.ItemTemplate>-->
        </CollectionView>
    </RefreshView>
</ContentPage>